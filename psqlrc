\set QUIET 1
\set PROMPT1 '%M:%> %n@%/%R%#%x '
\set COMP_KEYWORD_CASE upper
\set VERBOSITY verbose
\setenv PAGER 'pspg -I -g --style=16 --bold-labels --reprint-on-exit'
\pset null '(ω)'
\pset linestyle unicode
\pset border 2
\timing
-- \set ECHO_HIDDEN on
\set ON_ERROR_STOP on
\unset QUIET

SET search_path TO public,aps,contracts,dealsworkflow,dms,funds,termcatalog,acs;
